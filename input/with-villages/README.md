# input/with-villages

آرشیو کامل چک‌لیست اداری استان‌ها **همراه با فهرست روستاها**.

- فایل‌های فعال پروژه در `input/{n}.json` هستند و **بدون روستا** (`villages: []`).
- این پوشه فقط برای بایگانی/مرجع است؛ `import_province_scopes` از `input/{n}.json` می‌خواند، نه از اینجا.
