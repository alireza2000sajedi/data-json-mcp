# PLANRO — FULL PROVINCE (بدون Scope)

کل استان را **یک‌سره و کامل** از `input` پر کن.
فقط `province_id`. **`scope_id` نخواه.** برای Scope بعدی نایست. پرامپت `02` لازم نیست.

قرارداد مادر برای جزئیات schema/brand: `01-start-province.txt` — ولی این فایل برای اجرای کامل استان **کافی و مستقل** است.

### RUN VARIABLES
```text
province_id=<PROVINCE_ID>
```
- مقدار واقعی در Task authoritative است (عدد یا `province-{n}`).
- نبود → `MISSING_INPUT: province_id` و توقف؛ سؤال نپرس.
- عدد را به `province-{n}` normalize کن.
- از کاربر نام شهرستان / شهر / روستا / `scope_id` نخواه.

---

## 0) قانون — اداری فقط input؛ Place با تحقیق
**واحد اداری** (County / City / Village) را **فقط** از `input/{n}.json` بگیر (`import_province_scopes` / `planro://scopes/{provinceId}`).
- شهرستان/شهر/روستای جدید نساز.
- اگر ScopeUnit فیلد `location` داشت، همان lat/lng مبنای Entity است.

**Place (جاذبه‌ها) تحقیق اجباری است** — checklist در input فقط بذر/اولویت است، نه سقف.
- `import` هر Place چک‌لیست را seed می‌کند؛ تا entity ذخیره نشود DoD پاس نمی‌شود.
- اول `placesChecklist` / Nodeهای seed‌شده را کامل کن.
- بعد وب را برای جاذبه‌های بیشتر جستجو کن؛ Place جدید زیر Parent موجود در input.
- `complete_discovery_task` با count < طول checklist → خطای `PLACES_BELOW_CHECKLIST` (ممنوع).
- `count:0` بدون ≥۳ جستجوی کشف Place → `PLACES_ZERO_WITHOUT_SEARCH`.

---

## 1) هدف — کل درخت، نه فقط Place
تا پایان این پرامپت باید کامل شوند:

| واحد | چه چیزی |
|---|---|
| Province | اگر هنوز ناقص است: entity + media=۵ + sources + FAQ + checklist |
| هر County | entity + media=۳ + City/Village/Placeهای زیر آن |
| هر City | entity + media=۳ + Village/Placeهای زیر آن |
| هر Village | entity + media=۲ + Placeهای زیر آن |
| هر Place | entity + media=۴ + Visit کامل |

محل کمپ = Place (`campground`) — Entity جدا نساز.

### سلسله‌مراتب (قفل)
- Village Parent = County یا City — **هرگز فقط Province**
  - `counties[].villages` → Parent = County
  - `counties[].cities[].villages` → Parent = City
- **روستا ≠ Place:** اول Village را کامل کن؛ جاذبه را Place زیر همان Village بگذار؛ `روستای X` ممنوع
- Place را زیر Parentی بگذار که در scopes/input وجود دارد (County یا City یا Village)
- در Entity: `location.province / county / city / village` مطابق زنجیره

### Place چه چیزی باشد
مجاز: اثر تاریخی شاخص، موزه، بازار تاریخی، کاخ، قلعه، آبشار، غار، دریاچه، پارک ملی/جنگلی مهم، زیارتگاه معروف گردشگری، برج/پل نمادین، محوطهٔ باستان، کمپینگ شاخص.
ممنوع: مسجد محلی معمولی، مغازه، پمپ‌بنزین، اداره، مدرسه، بیمارستان، صافکاری، رستوران زنجیره‌ای بی‌اهمیت.

### حداقل پوشش Place (هدف کیفیت)
برای هر Parent قبل از بستن track `places` / `countyPlaces`:
1. همهٔ نام‌های checklist input
2. به‌علاوهٔ جستجوی وب تا جاذبه‌های مهم پوشش داده شوند
3. شهر مرکز شهرستان / شهرهای گردشگرپذیر: معمولاً چند Place واقعی — با ۱–۲ تا زود متوقف نشو
4. شهرک اقماری بدون جاذبهٔ مستند: `count:0` با جستجوی واقعی ثبت‌شده مجاز است

---

## 2) قفل اجرا — continuous؛ بدون Scope Selection
1. MCP/Resourceها طبق ۰۱ (اگر آماده است تکرار بی‌نیاز).
2. `import_province_scopes(provinceId)` — درخت را از input بساز؛ deep-research برای کشف واحد جدید نکن.
3. `set_active_scope(provinceId)` — حالت **full-province / continuous**.
4. صف: `list_pending_nodes({ provinceId, allScopes: true })` + `get_next_research_node`.
5. ترتیب DFS را حدس نزن.
6. **ممنوع:** `awaitingScopeSelection` را به توقف تبدیل کن · Scope بعدی را از کاربر بپرس · بعد از یک county بایست · به `02` سوئیچ کن

```text
output/{province_id}/
├── province-*.json
└── county-*/
    ├── county entity
    ├── place-*.json
    ├── city-*/
    │   ├── city entity
    │   ├── place-*.json
    │   └── village-*/          # Village زیر City
    │       ├── village entity
    │       └── place-*.json
    └── village-*/              # Village مستقیم زیر County
        ├── village entity
        └── place-*.json
```

Discovery tracks (count واقعی؛ خالی = `0`):
- province → `provincePlaces`
- county → `cities` · `villages` · `countyPlaces` · `districts`/`ruralDistricts`
- city → `places` · `villages` (اگر در input زیر city آمده)
- village → `places`

---

## 3) TURBO + کیفیت (همان Node)
سریع‌تر تمام کن، نه ساده‌تر.

مجاز: تحقیق موازی یک Node (۵ Primary + مختصات + media) · `discover_subtree` · Queryهای fa/en · توقف media در target

ممنوع: حذف Place/Source/media برای سرعت · کم‌کردن Coverage یا media target · دانلود/ذخیرهٔ فایل عکس · چند `save_active_entity` هم‌زمان · ساخت واحد خارج از input · commit با fail

### حلقهٔ هر Node — کیفیت نهایی همان‌جا
1. `get_node_context(nodeId)` — checklist را بخوان؛ فقط بذر است
2. `discover_subtree` برای Queryهای Place همان Node / زیردرخت
3. برای County/City/Village: جستجوی Place را اجرا کن (نه فقط checklist) → نام‌های معتبر را به‌عنوان Place زیر همین Parent save کن
4. `record_search_result` × ۵ Primary تا `get_source_coverage` → `satisfied:true`
5. متن نهایی انسانی (§۸ پرامپت ۰۱)
6. Media تا target: Province=۵ · County=۳ · City=۳ · Village=۲ · Place=۴ — **فقط URL**؛ دانلود فایل ممنوع
7. `save_active_entity` — فقط MCP
8. اگر خطا → همان Node را تعمیر کن؛ ناقص رد نشو
9. `complete_discovery_task({ nodeId, track, count })` — count = تعداد Place واقعی ذخیره‌شده (نه صرفاً طول checklist)

Ownership: Parent فقط معرفی کوتاه Child؛ بدون ticket/hours/FAQ عملیاتی Child.
`costs` / `evidence` وارد Entity نکن.
Taxonomy: فقط Global IDs؛ مفهوم جدید → `taxonomy/agent-taxonomy/` (بدون گذاشتن id در Entity تا promote).

Visit:
- Province/County: bestSeasons/bestMonths — openingHours و فیلدهای Place ممنوع
- City/Village: + crowdLevel با منبع — openingHours عملیاتی Place ممنوع
- Place: Visit کامل

---

## 4) Call Order
1. `import_province_scopes(provinceId)`
2. `set_active_scope(provinceId)` — continuous / allScopes
3. `get_next_research_node` / `list_pending_nodes({ allScopes: true })`
4. `get_node_context(nodeId)`
5. `record_search_result` × ۵ → `get_source_coverage` satisfied
6. `record_media_candidate` → `finalize_media` تا target
7. `save_active_entity`
8. `complete_discovery_task`
9. تا pending کل استان صفر شود تکرار کن
10. `check_definition_of_done` → `validate_province`

`mark_node_complete` مستقیم نیست. نام→id از `planro://scopes` / `indexByName`.

---

## 5) پایان — کل استان
قبل از تحویل همه باید صفر/پاس باشند:
- `pendingByType` برای county / city / village / place
- trackهای باز Discovery
- `check_definition_of_done` → `complete:true` (full province)
- `validate_province` → `invalid:0`

اگر DoD به‌خاطر واحد اداری ناقص گیر کرد → **همان را در همین ران** تعمیر کن؛ به `02` موکول نکن.

ممنوع: توقف بعد از یک county · پرسیدن Scope · save بدون MCP · commit با `complete:false`/`invalid>0` · ساخت County/City/Village خارج از input · بستن Place track با ۰ بدون جستجوی واقعی

---

## 6) تحویل
گزارش: counties، cities، villages، places، taxonomy staging (چه idهایی در agent-taxonomy ساختی)

1. `check_definition_of_done` → `complete:true`
2. `validate_province` → `invalid:0`
3. اگر پاس نشد → **commit ممنوع**
4. پس از پاس: `git status --short output/` → `git add output/` → commit → `git push`
