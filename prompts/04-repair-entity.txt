# PLANRO — REPAIR ONE ENTITY

قرارداد مادر: `01-start-province.txt`.

### RUN VARIABLES
```text
province_id=<PROVINCE_ID>
scope_id=<SCOPE_ID>
entity_id=<ENTITY_ID>
previous_id=<PREVIOUS_ID>
```
- `entity_id` واقعی در Task authoritative است؛ نبود → `MISSING_INPUT: entity_id`.
- بقیه در صورت نیاز برای قفل Scope؛ نبود هرکدام که برای save لازم است → `MISSING_INPUT: <variable>`.

---

## اجرا
1. در صورت نیاز `set_active_scope(scope_id)`.
2. فقط همین `entity_id` را repair کن:
   - schema / hierarchy / Visit applicability
   - source coverage (۵ Primary)
   - FAQ / Checklist / Media مستقل همان Entity
   - media target: Province=۵، County=۳، City=۳، Place=۴
3. Child-data را به Parent منتقل نکن؛ URL عکس را بین Entityها reuse نکن.
4. اگر متن ماشینی یا نام OTA دارد → مطابق بخش ۸ پرامپت ۰۱ از نو بنویس.
5. `save_active_entity` → `validate_province` → `check_definition_of_done`.
6. متوقف شو؛ سؤال نپرس.

---

## تحویل
1. `check_definition_of_done` → `complete:true`
2. `validate_province` → `invalid:0`
3. اگر پاس نشد → **commit ممنوع**
4. پس از پاس: `git status --short output/` → `git add output/` → commit → `git push`
