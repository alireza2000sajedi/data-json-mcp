# PLANRO — REPAIR ONE ENTITY

قرارداد اصلی در `01-start-province.txt` است.

### RUN VARIABLES
```text
province_id=<PROVINCE_ID>
scope_id=<SCOPE_ID>
entity_id=<ENTITY_ID>
previous_id=<PREVIOUS_ID>
```
اگر در Task جاری مقدار واقعی `entity_id` آمده باشد، همان authoritative است و placeholder بالا نادیده گرفته می‌شود؛ در نبود مقدار واقعی `MISSING_INPUT: entity_id` بده و متوقف شو.

فقط همین Entity را repair کن. schema/taxonomy/hierarchy/source و applicability فیلدهای Visit/FAQ/Checklist/Media را بررسی کن.
Child-specific data را به Parent منتقل نکن و عکس Entity دیگری را reuse نکن.
پس از Save، validation و DoD را اجرا کن و متوقف شو.
سؤال تعاملی نپرس.

## تحویل (Delivery)
1. `check_definition_of_done` → `complete:true` و `validate_province` → `invalid:0`.
2. اگر DoD پاس نشد، **commit ممنوع**.
3. پس از پاس: `git status --short output/` → `git add output/` → commit → `git push`.
