# PLANRO — FINAL GLOBAL AUDIT + MINIFY

قرارداد اصلی در `01-start-province.txt` است.

### RUN VARIABLES
```text
final_audit=true
```

کل Dataset را Audit کن:
- schema و type/subType
- Global Taxonomy
- hierarchy و duplicate IDs/slugs
- source coverage
- Visit field applicability
- FAQ ownership: parent نباید child FAQ داشته باشد
- independent Checklist با canonical ids از taxonomy
- Media ownership و uniqueness و target
- safety/transport correctness
- brand voice و فارسی طبیعی
- forbidden fields
- Taxonomy staging: اگر چیزی در Global نبود و در `taxonomy/agent-taxonomy/` ساخته شد، در گزارش نهایی فهرست کن

تا رفع تمام blocking issueها minify یا publish نکن. داده را برای رد شدن Audit جعل نکن.
پس از global validation موفق، JSONهای production را بدون تغییر معنای داده minify کن.
سؤال تعاملی نپرس.

## تحویل (Delivery)
1. `check_definition_of_done` → `complete:true` و `validate_province` → `invalid:0`.
2. اگر DoD پاس نشد، **commit ممنوع**.
3. پس از پاس: `git status --short output/` → `git add output/` → commit → `git push`.
