# PLANRO — FINAL AUDIT + MINIFY

آخرین مرحلهٔ استان. قرارداد مادر: `01-start-province.txt`.
متن و media باید از قبل در `01`/`07` (یا `02` اگر Scope تکی بود) نهایی شده باشند؛ اینجا فقط Audit + بستن شکاف‌های باقی‌مانده + minify.

### RUN VARIABLES
```text
final_audit=true
```

---

## 1) Audit
- schema / type / subType
- Global Taxonomy
- hierarchy / duplicate id-slug
  - Village Parent = County یا City (نه Province تنها)
  - Place زیر Village نباید نام خود روستا باشد
- source coverage (۵ Primary)
- Visit applicability (City/Village vs Place)
- FAQ ownership (بدون child-FAQ در Parent)
- Checklist با idهای taxonomy
- Media: ownership، uniqueness، target
  - Province=۵ · County=۳ · City=۳ · Village=۲ · Place=۴
- brand voice + نبود OTA در متن کاربرمحور (§۸ پرامپت ۰۱)
- forbidden fields (`costs` / `evidence`)
- فهرست staging در `taxonomy/agent-taxonomy/`

اگر متن ماشینی یا media زیر target دیدی → **همان‌جا** درست کن (نه پرامپت جدا).

---

## 2) Media Completion (اگر هنوز gap مانده)
Entity زیر target را به target برسان. TURBO: در target متوقف شو.
`record_media_candidate` → `finalize_media` → `save_active_entity`.
**فقط URL** — دانلود یا ذخیرهٔ باینری تصویر ممنوع.
reuse URL ممنوع. deficit فقط وقتی هیچ تصویر قابل‌اتکا نیست.

---

## 3) Cleanup
پاک کن: tmp، draft، review، scratch، backup، `.DS_Store`، log، فایل آزمایشی.
هرگز پاک نکن: Entityهای `output/`، notes، `input/`، `taxonomy/`، `dataset/`، `prompts/`، `src/`، `sources[]` معتبر.

---

## 4) Minify + تحویل
تا blocking issue رفع نشده minify نکن. داده جعل نکن.
1. `check_definition_of_done` → `complete:true`
2. `validate_province` → `invalid:0`
3. اگر پاس نشد → **commit ممنوع**
4. پس از Cleanup و پاس: JSONها را minify کن؛ سپس `git status --short output/` → `git add output/` → commit → `git push`
سؤال نپرس.
