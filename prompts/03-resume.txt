# PLANRO — RESUME ONE SCOPE

قرارداد مادر: `01-start-province.txt`. اجرای Scope مثل `02-run-scope.txt`.

### RUN VARIABLES
```text
province_id=<PROVINCE_ID>
scope_id=<SCOPE_ID>
previous_id=<PREVIOUS_ID>
```
- مقدار واقعی در Task authoritative است.
- نبود `previous_id` → `MISSING_INPUT: previous_id` و توقف.
- نبود `province_id` / `scope_id` → `MISSING_INPUT: <variable>`.

---

## اجرا
1. `set_active_scope` با همان `scope_id`.
2. State را از MCP بخوان (`get_scope_state` / `list_pending_nodes` / `get_next_research_node`).
3. از اولین Node ناقص **بعد از** `previous_id` ادامه بده؛ کار کامل را بی‌دلیل تکرار نکن.
4. همان قواعد ۰۱/۰۲ با TURBO SPEED / SPEED MODE: sources، media (County=۳، City=۳، Place=۴)، ownership، taxonomy، متن انسانی.
5. محل کمپ = Place؛ Entity جدا نساز.
6. بعد از تکمیل همان Scope متوقف شو؛ سؤال نپرس.

---

## تحویل
1. `check_definition_of_done` → `complete:true`
2. `validate_province` → `invalid:0`
3. اگر پاس نشد → **commit ممنوع**
4. پس از پاس: `git status --short output/` → `git add output/` → commit → `git push`
