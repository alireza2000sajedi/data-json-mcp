# Global Planro Taxonomy

Taxonomy is global and shared by every province and every Entity. Never create province-local taxonomy files.

Production Entity fields must contain canonical taxonomy IDs only. Missing reusable concepts may be proposed in `agent-taxonomy/proposals.json`; pending proposals are never valid production values until manually promoted.
