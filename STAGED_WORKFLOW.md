# اجرای مرحله‌ای (Stage-Based) و شناسه‌های اختصاصی Scope

> این فایل قرارداد اجرای پلکانی پروژه Planro است. هر اجرای Agent دقیقاً **یک Scope** دارد؛
> پس از پایان همان Scope، Agent متوقف می‌شود و منتظر دستور بعدی کاربر می‌ماند.

---

## ۱. شناسه‌های اختصاصی (Scope IDs)

هر واحد جغرافیایی یک **id پایدار و قطعی** دارد که از چک‌لیست مرجع `input/{n}.json` گرفته می‌شود
(طبق `dataset/README.md` §5):

| سطح | الگو | مثال |
|---|---|---|
| استان | `province-{n}` | `province-30` (همدان) |
| شهرستان | `county-{p}-{n}` | `county-30-5` (فامنین) |
| شهر | `city-{p}-{n}` | `city-30-12` (فامنین) |
| روستا | `village-{p}-v{n}` | `village-30-v2` |
| POI/Place | `place-{p}-{n}` | `place-30-3` (در زمان Deep Research اختصاص می‌یابد) |

قواعد:

- `{n}` شهرستان = ایندکس ۱-پایه داخل استان.
- `{n}` شهر و `v{n}` روستا = شمارهٔ سراسریِ استان (ترتیب فایل input) تا حتی نام‌های تکراری
  (مثلاً نام روستای مشترک در دو شهرستان) **id یکتا** داشته باشند.
- idها **قطعی (deterministic)** هستند: هر بار همان خروجی تولید می‌شود؛ Resume هیچ ابهامی ندارد.
- دستور کاربر با نام هم قابل ترجمه است: `استان همدان، فامنین را انجام بده` ≡
  `province-30` → `county-30-5`.

## ۲. منابع ID (Scope Registry)

| Resource | محتوا |
|---|---|
| `planro://scopes` | ایندکس ۳۱ استان: `provinceId`، نام، تعداد و `countyId` هر شهرستان |
| `planro://scopes/{provinceId}` | رجیستری کامل: `tree` (سلسله‌مراتب)، `index` (id → واحد)، `indexByName` (نام → idها) |

ففط خواندنی است؛ هیچ تحقیق یا فایل Entity‌ای نمی‌سازد.

## ۳. قرارداد هر اجرا (یک Scope در هر اجرا)

### Scope A — استخراج ساختار استان (Province Discovery)

1. کاربر فقط استان را می‌گوید (مثلاً `استان همدان`).
2. `import_province_scopes` را صدا بزن (یا `planro://scopes/{provinceId}` را بخوان).
3. `investigation` نمی‌شود: **نه Deep Research، نه POI، نه Entity، نه هیچ شهر/شهرستانی**.
4. خروجی را گزارش کن و **کاملاً متوقف شو**.

### Scope B — Deep Research یک شهرستان/شهر

1. کاربر می‌گوید: `همدان → فامنین`.
2. `set_active_scope` با `nodeId` همان واحد (مثلاً `county-30-5`).
3. فقط زیردرخت همان Scope فعال می‌شود (`get_next_research_node`/DFS روی همان Scope قفل می‌شود).
4. تحقیق کامل Scope («فامنین + زیرمجموعه‌های لازم»)، ساخت فایل‌ها، ذخیره، Checkpoint و `mark_node_complete`.
5. **توقف**؛ Scope بعدی فقط با دستور کاربر.

### Scope C / D — روستا یا POI

همان الگو با `village-{p}-v{n}` یا `place-{p}-{n}`. هیچ واحد همسایه یا والد بررسی نمی‌شود.

## ۴. قوانین غیرقابل‌نقض

- هر اجرا **یک Scope**؛ سراغ Scope دیگر نرو و اطلاعات آن را فقط در notes نگه دار.
- پس از پایان Scope: فایل‌ها + وضعیت ذخیره شود، Scope `completed` شود، اجرا متوقف شود، منتظر دستور بمان.
- **Resume:** ابتدا `get_scope_state`/`get_next_research_node` را بخوان؛ فقط Scope ناتمام بعدی را اجرا کن؛
  فایل تکمیل‌شده دوباره تولید نشود مگر با دستور صریح.
- `set_active_scope` با `nodeId: null` بازگشت به حالت استان‌محور است.
- `mark_node_complete` برای نودهای خارج از Scope فعال با خطای `SCOPE VIOLATION` رد می‌شود.
